# 林肯面板：自动超车（Auto Overtake, Experimental）复刻文档
## 0. 功能定义
- 高速场景自动发起变道超车，并在满足条件后自动回到原车道。
- 仅注入“变道意图”，不改变纵向控制（ACC/openpilot 纵向仍由原逻辑决定）。

## 1. 必须涉及的文件清单
- `common/params_keys.h`
- `selfdrive/ui/layouts/settings/lincoln.py`
- `selfdrive/ui/onroad/lane_pref_button.py`
- `selfdrive/ui/onroad/hud_renderer.py`
- `selfdrive/controls/lib/auto_overtake.py`
- `selfdrive/controls/controlsd.py`
- `selfdrive/controls/lib/desire_helper.py`
- `selfdrive/modeld/modeld.py`
- `selfdrive/ui/soundd.py`
- `selfdrive/modeld/coned.py`
- `selfdrive/modeld/cone_detections.py`
- `selfdrive/modeld/lane_occupancy.py`

## 2. 参数（`common/params_keys.h`）
- `dp_lincoln_auto_overtake`：PERSISTENT BOOL，默认 `0`
- `dp_lincoln_auto_overtake_min_cruise_kph`：PERSISTENT INT，默认 `90`（范围 60~140 km/h）
- `dp_lincoln_auto_lc_confirm_delay_sec`：PERSISTENT INT，默认 `3`（范围 0~10 秒）
- `dp_lincoln_lane_preference`：PERSISTENT INT，默认 `0`
  - 0=AUTO, 1=KEEP LEFT, 2=KEEP RIGHT
- `dp_lincoln_auto_lc_edge_clearance_m`：PERSISTENT STRING，默认 `0.6`（范围 0.3~2.0 m）
  - 自动变道时用作“路沿安全间距”门槛（仅影响自动变道，手动打灯不受影响）。

## 3. Lincoln 面板 UI（`selfdrive/ui/layouts/settings/lincoln.py`）
- Toggle：`Auto overtake` → `dp_lincoln_auto_overtake`
- Spin：`Auto lane-change min speed` → `dp_lincoln_auto_overtake_min_cruise_kph`（60~140，步进 1，km/h）
- Spin：`Auto lane-change confirm delay` → `dp_lincoln_auto_lc_confirm_delay_sec`（0~10，步进 1，sec）
- Spin：`Auto LC edge clearance` → `dp_lincoln_auto_lc_edge_clearance_m`（0.3~2.0，步进 0.1，m）

## 4. HUD 车道偏好按钮（`lane_pref_button.py` + `hud_renderer.py`）
### 4.1 显示门槛
- 仅当 `carParams.brand == "ford"` 且（`dp_lincoln_auto_avoid` 或 `dp_lincoln_auto_overtake`）开启时显示。

### 4.2 按钮行为
- 单击循环：`A -> L -> R`（写入 `dp_lincoln_lane_preference`）。
- 必须写入 **INT**（`Params.put_nonblocking`）。
- 说明：A/L/R 是车道偏好（AUTO/KEEP LEFT/KEEP RIGHT），不是“左/中/右车道选择”。

### 4.3 视觉样式（1:1）
- 背景：半透明黑 `rl.Color(0,0,0,110)`，圆形渐变。
  - 按下态外圈 alpha=150，未按下 alpha=110；中心 alpha=外圈 * 0.55。
  - 阴影：偏移 `(2, 3)`，alpha 未按下 55、按下 80。
- 文字：`A/L/R`，白色字体 + 黑色阴影。
  - 字号：`font_size = 68`。
  - 阴影偏移 `(2, 2)`，alpha=120。
- 字体 alpha：按下 180，未按下 255。
- 强调环颜色：
  - AUTO：`(255,255,255,120)`
  - KEEP LEFT：`(0,122,255,160)`
  - KEEP RIGHT：`(255,149,0,160)`
- 强调环厚度：
  - `ring_outer = radius - 1`
  - `ring_inner = ring_outer - max(3, radius * 0.06)`
  - 额外内环：`ring_inner-2` 到 `ring_inner-1`，alpha=35
- 半径与尺寸：`hud_renderer` 中 `UI_CONFIG.button_size * 0.85`。

### 4.4 HUD 位置
- 左侧列，介于“MAX 设定速度框”和“驾驶员监控图标”之间。
- X 与驾驶员监控图标中心对齐。

## 5. 自动超车状态机（`selfdrive/controls/lib/auto_overtake.py`）
### 5.1 常量
- `OVERTAKE_MIN_SPEED = 80 km/h`
- `OVERTAKE_MIN_CRUISE_SPEED = 90 km/h`（fallback 默认值）
- `dp_lincoln_auto_overtake_min_cruise_kph`：最小巡航设定速度阈值（默认 90，范围 60~140 km/h）
  - 当该阈值低于 80 时，`v_ego` 的最低速度阈值同步降低（`min(80, min_cruise)`）。
- `OVERTAKE_SPEED_DELTA = 10 km/h`
- `OVERTAKE_HEADWAY_MAX_S = 3.5`
- `OVERTAKE_LEAD_STABLE_SEC = 1.0`
- `PREPARE_BEFORE_LC_SEC = 0.6`
- `RETURN_CLEAR_DELAY_SEC = 5.0`
- `RETURN_MIN_TIME_AFTER_OUT_SEC = 4.0`
- `OVERTAKE_COOLDOWN_SEC = 20.0`
- `CLEAR_LANE_STABLE_SEC = 0.6`

### 5.2 触发条件（need_overtake）
- `lead_present=True` 且 `lead_d > 0`
- `v_cruise >= dp_lincoln_auto_overtake_min_cruise_kph`
- `v_ego >= min(OVERTAKE_MIN_SPEED, dp_lincoln_auto_overtake_min_cruise_kph)`
- `headway = lead_d / v_ego <= OVERTAKE_HEADWAY_MAX_S`
- `v_cruise - v_lead >= OVERTAKE_SPEED_DELTA`
- 条件持续 ≥ `OVERTAKE_LEAD_STABLE_SEC`

### 5.3 状态机
- `idle -> preparing -> changing_out -> holding/waiting_return -> changing_back`
- `preparing`：等待 `PREPARE_BEFORE_LC_SEC`
- `holding`：当 lane_preference 要求“保持超车道”时不回原车道
- `waiting_return`：need_overtake=False 且延迟满足后返回
- `cooldown`：完成后等待 `OVERTAKE_COOLDOWN_SEC`

### 5.4 方向选择
- LHD 默认左超车，RHD 默认右超车。
- `lane_preference` 仅在“偏好与超车方向一致”时保持超车道：
  - LHD + KEEP LEFT → 保持超车道
  - RHD + KEEP RIGHT → 保持超车道
  - 其它组合 → 按 `waiting_return` 返回原车道

### 5.5 手动转向灯门槛
- `manual_blinker=True` 且状态为 `idle` 时，不进入超车流程。

## 6. modeld 总门控（`selfdrive/modeld/modeld.py`）
### 6.1 启用条件
- `dp_lincoln_auto_overtake == 1`
- `latActive == True`
- `cruiseState.enabled == True`
- `bsm_available == True`

### 6.2 目标车数据来源
- `radarState.leadOne`（`status==True`）
- 若 `vLead` 无效 → `v_ego + vRel`

### 6.3 车道可用性门槛
- 基础条件：
  - BSM 可用（`carParams.enableBsm`）
  - blindspot == False
  - RoadEdgeDetector 未检测到路沿
- 路沿安全间距（新增）：
  - 基于 `roadEdges + laneLines` 估算“路沿到当前车道边界”的距离；
  - 若该距离 `< dp_lincoln_auto_lc_edge_clearance_m` → 禁止自动变道（不影响手动打灯）。
- 说明：相邻车道占用判断基于视觉+车道线；前向无左/右侧传感器，BSM 仅后方盲区。
- 目标车道阻塞（来自 coned）：
  - `leftLaneHazDistM/rightLaneHazDistM < target_lane_block_dist_m` → 禁止变道
  - `target_lane_block_dist_m = clamp(v_ego * TGAP, MIN, MAX)`
    - `DP_LINCOLN_AUTO_LC_TGAP_S` 默认 `2.0`
    - `DP_LINCOLN_AUTO_LC_MIN_DIST_M` 默认 `25.0`
    - `DP_LINCOLN_AUTO_LC_MAX_DIST_M` 默认 `90.0`
  - `DP_LINCOLN_AUTO_LC_BLOCK_HOLD_SEC` 默认 `0.8`
- 并行车辆阻塞：
  - `DP_LINCOLN_AUTO_LC_SIDE_Y2_MIN_FRAC`
  - `DP_LINCOLN_AUTO_LC_SIDE_AREA_MIN_FRAC`
  - `DP_LINCOLN_AUTO_LC_SIDE_X_GUARD_FRAC`
  - `DP_LINCOLN_AUTO_LC_SIDE_BLOCK_HOLD_SEC`
  - `DP_LINCOLN_AUTO_LC_SIDE_CLOSE_DIST_M` 默认 `15.0`
- 车道线门槛（lane_lines_ok_for_auto_lc）：
  - `DP_LINCOLN_AUTO_LC_LANE_LINE_PROB_MIN` 默认 `0.30`
  - `DP_LINCOLN_AUTO_LC_LANE_MARGIN_M` 默认 `-0.20`

### 6.4 额外门槛（强制安全）
- `det_payload` 超过 1s 未更新或 lane_lines_ok_for_auto_lc=False → 禁止 *自动* 变道。
- 曲率保护：
  - `k_cur = abs(yawRate)/v_ego`
  - v0=22 m/s, v1=35 m/s, k0=0.004, k1=0.002
  - `k_cur > k_th` 且未打灯 → 禁止启动自动变道。

### 6.5 与自动避让优先级
- 若 `avoid_dir` 存在 → 优先于 `overtake_dir`。

### 6.6 变道完成冷却
- `DP_LINCOLN_AUTO_LC_POST_FINISH_COOLDOWN_SEC = 1.0`

### 6.7 自动打灯与确认延迟
- controlsd 在 `laneChangeState != off` 时自动打灯；仅自动变道会延迟打灯以保证语音领先。
- `AUTO_LC_BLINKER_DELAY_SEC = 0.3`（环境变量：`DP_AUTO_LC_BLINKER_DELAY_SEC`）。
- 自动变道确认延迟：`dp_lincoln_auto_lc_confirm_delay_sec`（默认 3 秒），
  实际等待时间 = `dp_lincoln_auto_lc_confirm_delay_sec + AUTO_LC_BLINKER_DELAY_SEC`。
- 相关文件：`selfdrive/controls/controlsd.py`、`selfdrive/controls/lib/desire_helper.py`、`selfdrive/ui/soundd.py`

### 6.8 安全门槛与限制（设计意图，不是缺陷）
- BSM 为硬门槛：`enableBsm == False` 时不允许自动超车（仅后方盲区雷达能可靠判断侧后方占用）。
- 检测数据新鲜度门槛：`det_payload` 超过 1s 或 `lane_lines_ok_for_auto_lc=False` 时禁止自动变道（fail-closed）。
- 需要有效前车：`radarState.leadOne.status == True` 才会触发超车流程。
- 手动打灯只在 `idle` 阶段阻止自动超车启动；进入 `preparing/changing_out` 后不会因为打灯自动取消。

## 7. 语音提示（`selfdrive/ui/soundd.py`）
- 文件：自动变道提示优先 `left-side.wav/right-side.wav`，回退 `left.wav/right.wav`。
- 播放条件：
  - `dp_lincoln_auto_avoid` 或 `dp_lincoln_auto_overtake` 启用。
  - `selfdriveState.enabled == True`。
  - `modelV2.meta.laneChangeState` 在 `preLaneChange` 或 `laneChangeStarting`。
  - preLaneChange 时如果**驾驶员打灯**则不播（避免重复）；此时会在 `laneChangeStarting` 作为 fallback 播放。
- 最小间隔：`MANEUVER_VOICE_MIN_INTERVAL_S = 2.0`

## 8. 复刻步骤
1. 注册参数键（`dp_lincoln_auto_overtake`、`dp_lincoln_auto_overtake_min_cruise_kph`、`dp_lincoln_auto_lc_confirm_delay_sec`、`dp_lincoln_lane_preference`、`dp_lincoln_auto_lc_edge_clearance_m`）。
2. Lincoln 面板增加 `Auto overtake` 开关、最小速度调节、确认延迟调节、路沿安全间距调节。
3. HUD 添加 lane_preference 按钮与样式（位置/大小/颜色一致）。
4. `auto_overtake.py` 实现状态机（含全部常量）。
5. `modeld.py` 接入 overtake 逻辑与所有安全门槛，并读取最小速度参数。
6. `soundd.py` 增加自动变道语音提示。

## 9. 验证清单
- 高速慢车场景满足条件进入 preparing。
- lane_preference=KEEP LEFT/RIGHT 时不自动回原车道。
- 车道阻塞或检测数据过期时不触发自动变道。
- 语音提示在 preLaneChange/Starting 阶段触发，且最小间隔生效。
